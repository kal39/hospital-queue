services:
  - type: web
    name: hospital-queue-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    plan: free
    autoDeploy: true
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_NAME
        value: HospitalQueue
      - key: DATABASE_URL
        sync: false
      - key: JWT_ACCESS_SECRET
        sync: false
      - key: JWT_REFRESH_SECRET
        sync: false
      - key: JWT_ACCESS_EXPIRY_MINUTES
        value: 15
      - key: JWT_REFRESH_EXPIRY_DAYS
        value: 30
      # Set to * to allow all frontends, or your exact frontend URL e.g. https://your-app.vercel.app
      - key: CORS_ORIGINS
        value: "*"
      - key: RATE_LIMIT_MAX
        value: 100
      - key: RATE_LIMIT_EXPIRY_SECONDS
        value: 60
      - key: DB_MAX_OPEN_CONNS
        value: 25
      - key: DB_MAX_IDLE_CONNS
        value: 10
      - key: DB_CONN_MAX_LIFETIME
        value: 300
